import { AppHighlightOnFocusDirective } from './app-highlight-on-focus.directive';

describe('AppHighlightOnFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new AppHighlightOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
